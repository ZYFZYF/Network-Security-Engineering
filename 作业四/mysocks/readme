1. 建立连接
2. 签名认证
3. 完整性保护

服务器有一个会话私钥和一个会话公钥
客户端生成私钥、公钥
客户端将公钥发给服务器（会话公钥加密）
服务器用会话私钥解密得到客户端的公钥
服务器用公钥加密一个随机字符串发给客户端
客户端用私钥解密，（用会话密钥加密）发给服务器
服务端判断是否相符
认证通过

不停收发数据
数据加密方式：就简单置换加密先？或者直接用库的

数据完整性保护：
使用hash，把hash值放在数据后面，验证一下
需要同一个hash密钥：自己随便定一个吧

plan:
改一下加密方式；
改一下config内容。